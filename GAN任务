GAN的原理
1. matlab代码-实现手写字的识别
  1.1 说明GAN原理：参见wiki
  1.2 试试实验室数据：生成TOA测距误差
     已知：实际距离值；建模得到的距离值；利用 GAN生成的距离值；
     第一步: 已知距离，用TOA测距样本训练GAN（测量）， 生成对应距离下的TOA测距样本（假的），之前人体遮挡下的误差模型，生成对应距离下的TOA测距样本（仿真）
     网络结构调整； 输入数据调整（随机，100），
     第二步：有多个距离，距离对应下的TOA测距样本集合。GAN生成，某个距离下的TOA测距样本。
       for（某个距离：距离集合）   把不同样本值放在一起完成GAN，再生成TOA
           确认GAN
           
      距离是否要做为
        
